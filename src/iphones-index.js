import React from 'react';
import ReactDOM from 'react-dom/client';
import IphoneLandingPage from './components/IphoneLandingPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <IphoneLandingPage />
  </React.StrictMode>
);